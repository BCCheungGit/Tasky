/** @type {import('next').NextConfig} */
const nextConfig = {
    // Include any other existing Next.js config options you have
    typescript: {
      ignoreBuildErrors: true,
    }
  }

module.exports = nextConfig
